# 历史文档说明

根 README 曾混入大量按日期追加的 FDC 调试记录、旧 commit baseline、COM11/COM12 快照和已被新架构替代的协议说明。这些内容已从当前用户文档移除，避免旧记录被误认为当前契约。

需要追溯历史实现时，请使用 Git commit history 与对应 validation artifact。历史 build、flash 或 HIL 结果只证明当时的 commit、配置和硬件，不能自动继承到当前版本。
